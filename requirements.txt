# Speecher — проверенные версии (Python 3.11, Windows, venv в .venv).
#
# Установка с нуля:
#   python -m venv .venv
#   .venv\Scripts\python -m pip install torch --index-url https://download.pytorch.org/whl/cu128
#   .venv\Scripts\python -m pip install -r requirements.txt
#
# ВАЖНО: torch ставится ПЕРВЫМ и отдельным индексом (cu128 — сборка под
# Blackwell). С PyPI приедет +cpu, и движок nemotron работать не будет.
# faster-whisper на CUDA требует NVIDIA driver + cuBLAS/cuDNN 9 —
# ctranslate2 тянет их сам через pip-зависимости.

faster-whisper>=1.2.1
PyQt6>=6.11.0
numpy>=2.3
openai>=2.34          # один клиент на все провайдеры (OpenAI-совместимый REST)
keyring>=25.0         # API-ключи в диспетчере учётных данных Windows, не в config.toml
torch>=2.11           # CUDA-сборка, см. шапку

# --- Захват звука ---
# Питоновских зависимостей у него больше нет: звук берёт нативный бинарь
# native/audio_capture (Rust + WASAPI), он же делает downmix и ресемплинг.
# Нужен rustup со stable-x86_64-pc-windows-msvc, сборка:
#   cargo build --release --manifest-path native/audio_capture/Cargo.toml

# --- ASR-движок nemotron (config: [asr].engine = "nemotron") ---
# transformers дают AutoModelForRNNT/AutoProcessor; librosa и accelerate
# обязательны, но в карточке модели не указаны.
transformers>=5.13
librosa>=0.11
accelerate>=1.14

# --- Пунктуация (config: [punctuation].backend) ---
# silero (дефолт): torch + модель ~50 MB через torch.hub при первом запуске.
# Работает только для engine = "whisper" — nemotron пунктуирует сам.
# Второго бэкенда больше нет: deepmultilingualpunctuation удалён в фазе 3
# (нет русского, ~2.1 ГБ, тянет transformers ниже 5.13 и ломает nemotron).

# --- Перевод на лету (config: [translate].backend) ---
# nllb (дефолт): facebook/nllb-200-distilled-600M тянется с HF при первом
# включении (~2.5 ГБ). Ничего ставить не надо — transformers и torch уже есть,
# а токенизатор NLLB грузится из tokenizer.json, без sentencepiece.
# opusmt (Helsinki-NLP/opus-mt-{src}-{tgt}, ~300 МБ на пару) — MarianTokenizer
# требует двух пакетов, поэтому они НЕ в списке ниже, а ставятся по желанию:
#   .venv\Scripts\python -m pip install sentencepiece sacremoses
# Проверить модель вживую: .venv\Scripts\python scripts\translate_probe.py

# --- Dev ---
pytest>=8.0
