# Что нужно, чтобы прогнать tests/ — и больше ничего.
#
# Весь тяжёлый слой (torch, transformers, faster-whisper, librosa, accelerate,
# soxr, sentencepiece, keyring) импортируется ТОЛЬКО внутри функций: ни одного
# импорта на уровне модуля во всём src/. Поэтому тесты идут без GPU, без
# моделей и без 3 ГБ колёс, а CI укладывается в полминуты вместо десяти.
#
# Проверено фактом, а не на слово: прогон с этими пакетами, заблокированными
# через meta_path, даёт те же 309 зелёных. Если однажды CI начнёт падать на
# ImportError — значит, в src/ появился импорт на уровне модуля; это и есть
# сигнал, а не повод дописать сюда torch.

PyQt6>=6.11.0
numpy>=2.3
openai>=2.34
pytest>=8.0
