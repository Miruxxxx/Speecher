# Roadmap Speecher

Составлен 2026-07-15 по итогам ревью ([CODE_REVIEW_2026-07-15.md](CODE_REVIEW_2026-07-15.md)). Порядок этапов — рекомендация: стабильность и гигиена до новых фич.

## Этап 0 — Стабильность (блокер, до всего остального)

- [x] ~~Добить диагностику крэша B1~~ — сделано 2026-07-15: виновник PyAudioWPatch (idle loopback-стрим рушит кучу; Whisper чист — 5438 декодов без сбоя).
- [ ] Вынести захват аудио в отдельный подпроцесс (PCM через pipe/shared memory) с супервизором-перезапуском — падение PortAudio перестанет убивать приложение. Параллельно: проверить свежие версии/issues PyAudioWPatch, попробовать `soundcard` как альтернативу.
- [ ] Быстрая проверка гипотезы «крэш в простое»: держать рендер живым беззвучным стримом и посмотреть, исчезнут ли падения (диагностический костыль, не решение).
- [ ] Watchdog: супервизор перезапускает упавший пайплайн, UI показывает «reconnecting…».

## Этап 1 — Гигиена (1–2 вечера, кратно ускоряет всё дальнейшее)

- [ ] Конфиг-файл в `config/` (toml/yaml): устройство (name_hint), модель/девайс Whisper, язык, пунктуация on/off и её модель, URL и модель LM Studio, интервалы движка. Убрать хардкоды из `main.py`.
- [ ] Быстрый старт: окно сразу, модели грузятся в фоне со статусом в оверлее; `PunctuationModel` — в try/except (обрыв сети не должен ронять старт).
- [ ] LM Studio клиент: передавать `timeout` и `max_retries=0` в `openai.OpenAI`, health-check вне UI-потока, использовать `loaded_model_id()` вместо захардкоженного id.
- [ ] Удалить легаси (`src/transcript_engine.py`, `src/asr/streaming_worker.py`) и мёртвые тесты; написать тесты нового ядра: `GrowingAudioBuffer` (трим/офсеты), `_longest_agreed_prefix`, force-commit, `TranscriptStore` (+конкуррентная пере-пунктуация), `PunctuationWorker._tick` с фейковой моделью.
- [ ] `git rm -r --cached` для `__pycache__`, закоммитить `.gitignore`, `requirements.txt`, доки.

## Этап 2 — Качество распознавания

- [ ] Язык: зафиксировать в конфиге или sticky-детекция (определить по первым секундам речи, не переопределять каждый цикл). Сейчас это главный источник «каши» в транскрипте.
- [ ] Тишина: энергетический гейт до декода + трим буфера после N пустых декодов (сейчас 60 с тишины декодятся каждую секунду).
- [ ] Фильтры галлюцинаций: `no_speech_prob`, `avg_logprob`, повтор-детект в адаптере.
- [ ] Ресемплер с состоянием (soxr) вместо почанкового `resample_poly`.
- [ ] Прокачка модели: опция `large-v3-turbo` / `distil-large-v3` (для русского заметно лучше `base`); замер latency через существующий LatencyTracker.
- [ ] Пунктуация для русского: silero text-enhancement (CPU) или батчи через LM Studio; текущую модель — на CPU и только для EN/DE/FR/IT.

## Этап 3 — Функциональность

- [ ] Персист транскриптов в `storage/transcripts/` (папка уже создана): JSONL со словами и таймстемпами, автосейв, продолжение сессии.
- [ ] Экспорт в md/txt + копирование выделенного из оверлея.
- [ ] Глобальные горячие клавиши (Sum 5min, Last ?+AI) — не переключаясь в окно.
- [ ] Поиск вопроса через LLM вместо `rfind("?")` (устойчиво к кривой пунктуации).
- [ ] Автосаммари каждые N минут в фоне; «перевод на лету» (хелпер `translate()` уже написан).
- [ ] Мини-настройки в UI: выбор устройства, языка, модели.

## Этап 4 — Продукт

- [ ] Двухканальный режим: loopback + микрофон одновременно → дорожки «они»/«я» (диаризация по источнику бесплатно).
- [ ] Настоящая диаризация (pyannote.audio — зависимости в системе уже стоят).
- [ ] Упаковка: pyinstaller + трей-иконка + автозапуск.
- [ ] Профили промптов (митинг/лекция/интервью).

## Не делать (пока)

- Не тащить llama-cpp-python обратно in-process — REST-подход через LM Studio разгружает VRAM и процесс (см. крэш B1: чем меньше нативных стеков в одном процессе, тем лучше).
- Не менять алгоритм стабилизации на фаззи-слияние — LocalAgreement-2 работает и объясним.
