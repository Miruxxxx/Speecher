# Roadmap Speecher

Составлен 2026-07-15 по итогам ревью ([CODE_REVIEW_2026-07-15.md](CODE_REVIEW_2026-07-15.md)). Этапы 0–2 выполнены 2026-07-15 (вечер).

## Этап 0 — Стабильность ✅ (2026-07-15)

- [x] Диагностика крэша B1: виновник PyAudioWPatch (idle loopback-стрим рушит кучу; Whisper чист — 5438 декодов без сбоя).
- [x] Захват в отдельном подпроцессе: `capture_process.py` (ребёнок, только PortAudio) + `capture_supervisor.py` (рестарт с backoff, fast-fail детект). Проверено kill-тестом: ребёнок убит → перезапущен, данные пошли снова.
- [ ] (опционально) Попробовать `soundcard`/новые версии PyAudioWPatch — корневая причина остаётся в библиотеке; изоляция делает её нефатальной.

## Этап 1 — Гигиена ✅ (2026-07-15)

- [x] `config/config.toml` + `src/app_config.py`: все хардкоды из main.py вынесены (устройство, модель, язык, пунктуация, LM Studio, интервалы, фильтры).
- [x] Быстрый старт: окно мгновенно, Whisper в фоновом asr-loader-потоке со статусом в оверлее; ошибка загрузки — в статус, не крэш. Пунктуация lazy-load в своём потоке с graceful degradation.
- [x] LM Studio клиент: `timeout`/`max_retries=0`, health-check с коротким таймаутом вне UI-потока, `resolve_model()` (конфиг → первая загруженная), мёртвые хелперы удалены.
- [x] Легаси удалено (transcript_engine, streaming_worker, старый capture_stream, устаревшие тесты); новые тесты: 43 шт (buffer, engine, store, adapter, punctuation worker, config, sink).
- [x] `__pycache__` из индекса, .gitignore/requirements/доки закоммичены.
- [x] Сплиттер наполняет TranscriptStore напрямую — дроп UI-события больше не теряет слова.

## Этап 2 — Качество распознавания ✅ (2026-07-15)

- [x] Язык: режим `auto` = sticky-детекция (пин после первого уверенного декода), либо фиксированный в конфиге.
- [x] Тишина: RMS-гейт до декода + трим буфера после N пустых декодов.
- [x] Фильтры галлюцинаций: `no_speech_prob`/`avg_logprob` на сегментах + схлопывание повторов слова.
- [x] soxr.ResampleStream с состоянием вместо почанкового `resample_poly`.
- [x] Модель по умолчанию `large-v3-turbo` (конфигом меняется на base/small/…).
- [x] Пунктуация: silero-te (ru/en/de/es, CPU) по умолчанию; fullstop — опция, принудительно CPU.

## Этап 3 — Функциональность

- [ ] Персист транскриптов в `storage/transcripts/` (папка уже создана): JSONL со словами и таймстемпами, автосейв, продолжение сессии.
- [ ] Экспорт в md/txt + копирование выделенного из оверлея.
- [ ] Глобальные горячие клавиши (Sum 5min, Last ?+AI) — не переключаясь в окно.
- [ ] Поиск вопроса через LLM вместо `rfind("?")` (устойчиво к кривой пунктуации).
- [ ] Автосаммари каждые N минут в фоне; «перевод на лету» (хелпер `translate()` уже написан).
- [ ] Мини-настройки в UI: выбор устройства, языка, модели.

## Этап 4 — Продукт

- [ ] Двухканальный режим: loopback + микрофон одновременно → дорожки «они»/«я» (диаризация по источнику бесплатно).
- [ ] Настоящая диаризация (pyannote.audio — зависимости в системе уже стоят).
- [ ] Упаковка: pyinstaller + трей-иконка + автозапуск.
- [ ] Профили промптов (митинг/лекция/интервью).

## Этап 5 — Второй ASR-движок (Nemotron)

Спека: [MIGRATION_NEMOTRON.md](MIGRATION_NEMOTRON.md). Статус: гейт пройден, фаза 2 разблокирована.

- [x] Фаза 0 (2026-07-23) — разведка в отдельном venv: таймстемпы **есть** (`processor.decode(durations=…)`, кадр 80 мс, глобальные в стриминге), NeMo **не нужен** (`transformers` ≥5.13 + librosa + accelerate), переписывания **нет** (0 нарушений append-only на 4 конфигурациях), русский с нативной пунктуацией — но «?» не выставляется. Старт при этом не улучшается: 8.1–8.7 с против ~6 с у Whisper.
- [x] Фаза 1 (2026-07-23) — протокол `ASRBackend` + `asr.engine` в конфиге; Whisper обёрнут (`asr/whisper_backend.py`), поведение не меняется; `frame_sink` в супервизоре для будущего push-бэкенда.
- [ ] Фаза 2 — бэкенд Nemotron (без LocalAgreement-2, без буфера, без PunctuationWorker — пунктуация нативная).
- [ ] Фаза 3 — выкинуть `transformers`/`deepmultilingualpunctuation` из зависимостей (−1.1 с старта, бэкенд `deepmultilingual` всё равно без русского).

## Не делать (пока)

- Не тащить llama-cpp-python обратно in-process — REST-подход через LM Studio разгружает VRAM и процесс (см. крэш B1: чем меньше нативных стеков в одном процессе, тем лучше).
- Не менять алгоритм стабилизации на фаззи-слияние — LocalAgreement-2 работает и объясним.
